
<form method="post" action="{{ path('app_donations_delete', {'id': donations_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ donations_list.id) }}">
    <button class="btn">Delete</button>
</form>

