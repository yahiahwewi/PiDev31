<form method="post" action="{{ path('app_blog_post_delete', {'id': blog_post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="delete-form">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog_post.id) }}">
    <button type="submit" class="btn btn-danger btn-sm">Delete</button>
</form>
