{% extends 'base.html.twig' %}

{% block title %}Donator{% endblock %}

{% block body %}
    <h1>Donator</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ donator.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ donator.Nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ donator.Email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ donator.Password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_donator_index') }}">back to list</a>

    <a href="{{ path('app_donator_edit', {'id': donator.id}) }}">edit</a>

    {{ include('donator/_delete_form.html.twig') }}
{% endblock %}
