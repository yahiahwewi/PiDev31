{% extends 'base.html.twig' %}

<<<<<<< HEAD
{% block title %}Donator index{% endblock %}

{% block body %}
    <h1>Donator index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Email</th>
                <th>Password</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for donator in donators %}
            <tr>
                <td>{{ donator.id }}</td>
                <td>{{ donator.Nom }}</td>
                <td>{{ donator.Email }}</td>
                <td>{{ donator.Password }}</td>
                <td>
                    <a href="{{ path('app_donator_show', {'id': donator.id}) }}">show</a>
                    <a href="{{ path('app_donator_edit', {'id': donator.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_donator_new') }}">Create new</a>
=======
{% block title %}Donator's List{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="row justify-content-between align-items-center mb-4">
            <div class="col-md-6">
                <h1 class="mb-0">Donator's List</h1>
            </div>
            <div class="col-md-6 text-md-end">
                <form action="{{ path('app_donator_index') }}" method="GET" class="d-inline-flex align-items-center">
                    <div class="input-group">
                        <input type="text" name="search" class="form-control me-2" placeholder="Search..." aria-label="Search" value="{{ app.request.query.get('search') }}">
                        <button type="submit" class="btn btn-primary">Search</button>
                    </div>
                </form>
            </div>
        </div>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success" role="alert">
                {{ message }}
            </div>
        {% endfor %}

        {% if pagination is not empty %}
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th scope="col">ID</th>
                            <th scope="col">First Name</th>
                            <th scope="col">Last Name</th>
                            <th scope="col">Email</th>
                            <th scope="col">Montant</th>
                            <th scope="col">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for donator in pagination %}
                            <tr>
                                <td>{{ donator.id }}</td>
                                <td>{{ donator.Nom }}</td>
                                <td>{{ donator.Prenom }}</td>
                                <td>{{ donator.Email }}</td>
                                <td>{{ donator.Montant }}</td>
                                <td>
                                    <a href="{{ path('app_donator_show', {'id': donator.id}) }}" class="btn btn-sm btn-outline-primary me-2" title="Show Donator">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    <a href="{{ path('app_donator_edit', {'id': donator.id}) }}" class="btn btn-sm btn-outline-secondary" title="Edit Donator">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
            {{ knp_pagination_render(pagination) }} {# Ajout de la pagination #}
        {% else %}
            <p class="text-muted">No donators found</p>
        {% endif %}

        <div class="text-center mt-4">
            <button class="btn btn-primary" onclick="location.href='{{ path('app_donator_new') }}'" title="Create New Donator">
                <i class="fas fa-plus me-2"></i> Create new Donator
            </button>
        </div>
    </div>
>>>>>>> main
{% endblock %}
