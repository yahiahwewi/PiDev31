{% extends 'admin-base.html.twig' %}

{% block title %}Edit Projects{% endblock %}

{% block body %}
    <div class="card custom-card">
        <div class="card-body">
    <h1>Edit Projects</h1>
    <form action="{{ path('admin_projects_edit', {'id': project.id}) }}" method="post">
        <div class="row justify-content-center mt-4">
            <div class="col-md-6">
                {{ include('projects/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
    </form>

    

    <a href="{{ path('admin_projects_index') }}">back to list</a>

    {{ include('projects_admin/_delete_form.html.twig') }}
{% endblock %}
